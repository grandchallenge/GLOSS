# Test scaffold

Create Lean tests for expression traversal, source alignment, stable node IDs, normalization invariants, controlled rendering, loss partition completeness, proof-state deltas, and fixture serialization. Tests must fail before the corresponding work package is implemented; do not mark placeholders as passing implementations.
